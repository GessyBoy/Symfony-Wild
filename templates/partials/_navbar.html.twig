<nav class="bg-gradient-to-r from-gray-800 via-gray-900 to-gray-700 p-4 shadow-xl">
    <!-- Logo avec dégradé gris autour -->
    <a href="{{ path('app_index') }}">
        <div class="w-16 h-16 bg-gradient-to-br from-gray-300 via-gray-500 to-gray-800 rounded-full p-1 shadow-md transition-all duration-300">
            <img src="{{ asset('images/Toupa_sass.png') }}" alt="Logo" class="w-12 h-12 rounded-full border-2 border-white shadow-lg">
        </div>
    </a>

    <!-- Menu -->
    <ul class="flex space-x-8 justify-center items-center">
        <li>
            <a href="{{ path('app_index') }}" class="text-white font-semibold text-xl hover:text-gray-400 transition duration-300">Accueil</a>
        </li>
        <li>
            <a href="{{ path('program_index') }}" class="text-white font-semibold text-xl hover:text-gray-400 transition duration-300">Program</a>
        </li>
    </ul>
</nav>
