<nav class="bg-gradient-to-r from-gray-800 via-gray-900 to-gray-800 p-4 shadow-2xl">
    <div class="flex items-center justify-center space-x-8 max-w-screen-xl mx-auto">
        
        <!-- Lien Accueil -->
        <a href="{{ path('app_index') }}" class="text-white font-semibold text-xl hover:text-gray-400 transition duration-300">
            Accueil
        </a>

        <!-- Logo centré avec carré en dégradé -->
        <a href="{{ path('app_index') }}">
            <div class="w-20 h-20 bg-gradient-to-br from-gray-700 via-gray-500 to-transparent p-1 shadow-md rounded-md flex items-center justify-center">
                <img src="{{ asset('images/Toupa_sass.png') }}" alt="Logo"
                    class="w-16 h-16 rounded-full border-2 border-white shadow-lg object-cover" />
            </div>
        </a>

        <!-- Lien Program -->
        <a href="{{ path('program_index') }}" class="text-white font-semibold text-xl hover:text-gray-400 transition duration-300">
            Program
        </a>
    </div>
</nav>
