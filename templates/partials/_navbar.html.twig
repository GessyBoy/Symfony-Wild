<nav class="relative w-full h-32 p-6 bg-gradient-to-br from-red-800 via-orange-700 to-yellow-600">
    <!-- Conteneur du logo avec dégradé gris et les liens -->
    <div class="flex justify-center items-center w-full h-full relative bg-gradient-to-br from-gray-700 via-gray-500 to-transparent p-4 rounded-lg">
        <!-- Logo sans fond rond et avec bords arrondis -->
        <a href="{{ path('app_index') }}" class="absolute">
            <div class="w-24 h-24 bg-white p-1 rounded-lg flex items-center justify-center shadow-lg">
                <img src="{{ asset('images/Toupa_sass.png') }}" alt="Logo" class="w-20 h-20 rounded-lg border-2 border-white shadow-lg" />
            </div>
        </a>

        <!-- Liens autour du logo, dans le conteneur avec le dégradé -->
        <ul class="absolute flex items-center justify-center w-full h-full space-x-12">
            <!-- Accueil à gauche, sur le dégradé gris -->
            <li class="absolute left-1/4 transform -translate-x-1/2 top-1/2 -translate-y-1/2">
                <a href="{{ path('app_index') }}" class="text-white font-semibold text-xl hover:text-gray-300 transition duration-300">Accueil</a>
            </li>
            <!-- Program à droite, sur le dégradé gris -->
            <li class="absolute right-1/4 transform -translate-x-1/2 top-1/2 -translate-y-1/2">
                <a href="{{ path('program_index') }}" class="text-white font-semibold text-xl hover:text-gray-300 transition duration-300">Program</a>
            </li>
        </ul>
    </div>
</nav>
